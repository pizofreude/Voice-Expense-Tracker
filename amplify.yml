version: 1
backend:
  phases:
    build:
      commands:
        - npm ci
frontend:
  phases:
    build:
      commands:
        - echo "Building frontend assets"
  artifacts:
    baseDirectory: public
    files:
      - '**/*'